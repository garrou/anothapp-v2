export type FriendStatus = "friend" | "send" | "receive";

export type ProfileModal = "images" | "email" | "password";

export enum LayoutFieldFormat {
    Array = "array",
    Custom = "custom",
    Number = "number",
    Text = "text",
}